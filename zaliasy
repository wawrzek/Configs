alias ls='ls --color'

rdiff ()
{
	if [[ $1 = "-y" || $1 = "-u" ]]
	then
		option=$1
		shift
	else 
		option=""
	fi
	echo $option
	one=$1
	shift
	two=$1
	shift
	comm=$@
	echo "Compare \"$comm\" between $one and $two"
	diff $option =(ssh $one "$comm") =(ssh $two "$comm")	

}
